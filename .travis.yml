language: python
python:
  - "3.5"
  - "3.6"
install:
  - pip install python-coveralls
  - pip install coverage
  - pip install nose
script:
  - python3 -m unittest discover
  - nosetests --with-coverage
after_success:
  - coveralls
